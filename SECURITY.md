# 安全策略

## 支持版本

当前项目处于 `0.x` 阶段，安全修复优先面向最新版本。

## 报告安全问题

请不要在公开 issue 中披露可被利用的安全细节。优先通过 GitHub 私有漏洞报告能力提交；如果仓库暂未开启该能力，请联系维护者并提供：

- 影响范围
- 复现步骤
- 预期影响
- 建议修复方向

## Token 处理

memos-cli 不保存账号密码。配置文件中会保存 Memos Personal Access Token，并设置为仅当前用户可读写。CI 和自动化环境中推荐使用 `MEMOS_TOKEN` 环境变量。
